{
  "dependencies": {
    "@wasmer/sdk": "^0.9.0"
  },
  "devDependencies": {
    "vite": "^6.2.4",
    "vite-plugin-static-copy": "^2.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "caddy": "mise use caddy && open http://localhost ; caddy run -w"
  }
}
